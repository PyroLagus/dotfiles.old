alias ec='emacsclient'
alias du='du -hs'

# sudo aliases
alias sucp='sudo cp'
alias surm='sudo rm'
alias surmdir='sudo rmdir'
alias sumv='sudo mv'

## get top 10 shell commands:
alias top10='print -l ${(o)history%% *} | uniq -c | sort -nr | head -n 10'
